# Moonlight Peaks Internal Links

Status: PLAN_READY_DRAFT

## Navigation

Primary nav:

```text
Updates | Demo | Platforms | Release Date | Checklist | Sources
```

Footer nav:

```text
About | Editorial | Contact | Privacy
```

## Home Links

`/` links to:

- `/updates/` from the official updates card;
- `/demo/` from demo status card;
- `/switch-steam/` from platform matrix;
- `/release-date/` from regional date note;
- `/starter-checklist/` from first player action;
- `/sources/` from every source-confidence label;
- `/about/`, `/editorial/`, `/contact/`, `/privacy/` in footer.

## Core Page Links

`/updates/` links to:

- official Steam known-issues and patch announcements;
- the official release-date trailer on YouTube;
- `/demo/` for the carryover-status boundary;
- `/switch-steam/`, `/release-date/`, and `/starter-checklist/` for adjacent player tasks.

`/demo/` links to:

- `/switch-steam/` for platform comparison;
- `/release-date/` for launch context;
- `/starter-checklist/` for pre-play checklist;
- `/sources/` for source tiers and held claims.

`/switch-steam/` links to:

- `/demo/` for demo-specific platform availability;
- `/release-date/` for regional dates;
- `/sources/` for official source list.

`/release-date/` links to:

- `/switch-steam/` for platform differences;
- `/demo/` for playable demo status;
- `/sources/` for date source table.

`/starter-checklist/` links to:

- `/demo/` for demo status;
- `/switch-steam/` for platform choice;
- `/sources/` for held claims.

`/sources/` links back to all core pages and lists the future HOLD topics:

- romance gifts;
- dating routes;
- full character database;
- crop/recipe tables;
- exact demo walkthrough;
- save transfer/progress carryover.

## Link Rules

- Do not link to future HOLD URLs because they should not exist in v0.1.
- Do not use wiki/Fandom links as primary CTAs; if referenced, they belong only in source-risk context.
- Every factual card should show a source label and link to `/sources/`.
