{
  "name": "moonlight-peaks-compass",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "check": "astro check",
    "validate": "node scripts/validate-content.js",
    "clean": "node -e \"const fs=require('fs'); for (const p of ['dist','.astro']) fs.rmSync(p,{recursive:true,force:true});\""
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.2.1",
    "astro": "^4.16.0",
    "playwright": "^1.61.1"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.0",
    "typescript": "^5.5.0"
  }
}
